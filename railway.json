{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "sh -c 'python manage.py migrate && python manage.py collectstatic --noinput && python manage.py create_default_superuser && gunicorn --bind 0.0.0.0:$PORT --workers 3 huds_project.wsgi:application'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
